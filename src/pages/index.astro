---
import EditorialLayout from '../layouts/EditorialLayout.astro';
import EditorialHome from '../components/home/EditorialHome.astro';

const title =
  'Respostas práticas para os problemas digitais do trabalho | Estúdio Escrita Planejada';

const description =
  'Conteúdo gratuito sobre segurança digital, organização, inteligência artificial, ferramentas e produtos digitais para quem trabalha por conta própria.';

const canonicalPath = '/';
const baseUrl = Astro.site ?? Astro.url.origin;
const canonicalUrl = new URL(canonicalPath, baseUrl).toString();
const organizationId = `${canonicalUrl}#organization`;
const websiteId = `${canonicalUrl}#website`;
const webpageId = `${canonicalUrl}#webpage`;

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': organizationId,
      name: 'Estúdio Escrita Planejada',
      url: canonicalUrl,
      telephone: '+55 11 96450-2997',
      email: 'estudioescritaplanejada@gmail.com',
      areaServed: {
        '@type': 'Country',
        name: 'Brasil',
      },
      logo: new URL('/assets/img/logo.png', baseUrl).toString(),
      image: new URL(
        '/assets/img/og-estudio-escrita-planejada.jpg',
        baseUrl,
      ).toString(),
      taxID: '61.424.633/0001-11',
    },
    {
      '@type': 'WebSite',
      '@id': websiteId,
      url: canonicalUrl,
      name: 'Estúdio Escrita Planejada',
      description,
      inLanguage: 'pt-BR',
      publisher: {
        '@id': organizationId,
      },
    },
    {
      '@type': 'WebPage',
      '@id': webpageId,
      url: canonicalUrl,
      name: title,
      description,
      isPartOf: {
        '@id': websiteId,
      },
      about: {
        '@id': organizationId,
      },
      inLanguage: 'pt-BR',
    },
  ],
};
---

<EditorialLayout
  title={title}
  description={description}
  canonicalPath={canonicalPath}
  image="/assets/img/og-estudio-escrita-planejada.jpg"
  imageAlt="Estúdio Escrita Planejada: conteúdo prático para quem trabalha por conta própria"
>
  <script
    slot="head"
    is:inline
    type="application/ld+json"
    set:html={JSON.stringify(schema)}
  ></script>

  <script
    slot="head"
    is:inline
    async
    src="https://news.google.com/swg/js/v1/publisher.js"
  ></script>

  <EditorialHome />
</EditorialLayout>
